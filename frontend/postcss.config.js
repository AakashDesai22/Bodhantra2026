export default {
  plugins: {
    // We are using @tailwindcss/vite, so no tailwindcss plugin is needed here
    // But this file stops Vite from searching parent directories for stray configs
  },
}
